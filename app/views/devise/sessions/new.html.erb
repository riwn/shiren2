<% set_meta_tags title: "ログイン",description:"シレン2 タイムアタック番付サイトのログインページです" %>

<div class="container">
  <div class="row">
    <div class="offset-lg-3"></div>
    <div class="col-sm-12 col-lg-6">
      <div class="card bg-light mt-5 text-center">
        <h2 class="card-title float-left pt-3 pl-3">ログイン</h2>
        <div class="card-body">
          <div class="card-text">
            <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
            <div class="field">
              <%= f.label :name %><br />
              <%= f.text_field :name, autofocus: true, maxlength: 15,size: 30 %>
            </div>

            <div class="field">
              <%= f.label :password %><br />
              <%= f.password_field :password, autocomplete: "current-password",size: 30 %>
            </div>

            <% if devise_mapping.rememberable? %>
            <div class="field">
              <%= f.check_box :remember_me %>
              <%= f.label :remember_me %>
            </div>
            <% end %>

            <div class="actions">
              <%= f.submit "ログイン" ,{:class =>"btn btn-primary btn-lg mt-3 mb-3"} %>
            </div>
            <% end %>

            <%= render "devise/shared/links" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
  //ナタネ村を非表示する
  document.getElementById("nataneVillage").style.display = "none";
</script>